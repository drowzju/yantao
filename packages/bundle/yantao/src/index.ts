/**
 * @deepseek-ai/dsh-yantao — the yantao profile bundle: the intranet GLM
 * gateway provider route and default model selection as a patch layer. The
 * package's substance is `cordis.patch.yml`, declared by the
 * `dsh.bundle.patch` manifest field and resolved by the profile composer
 * through that field; this module carries no runtime API.
 * @module @deepseek-ai/dsh-yantao
 */

export {}
