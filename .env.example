# yantao 模型凭据模板。复制后填入真实值即可（`.env` 已被 .gitignore 忽略，本模板才入库）：
#   cp .env.example .env
#
# 模型网关的 API key（术语见 CONTEXT.md: LLM 网关 / Model Gateway）。
# 路由按角色命名——provider 为 `model-gateway`；至于网关背后是哪一家模型，
# 由 packages/bundle/yantao/cordis.patch.yml 的 endpoint 与模型 id 决定，
# 换模型改那里，与这里无关。
#
# 放置位置（二选一，或都写）：
#   1. <启动时的 cwd>/.env   —— 从本仓库目录启动时，就是本文件复制出的 .env
#   2. $DSH_HOME/.env        —— 默认 C:\Users\<你>\.dsh\.env，与启动目录无关
#
# 解析优先级（高 → 低）：
#   启动环境（export / 命令行前缀）
#   > $DSH_HOME/.credentials.yaml（UI「设置 → 模型」保存的，仅本用户可读）
#   > cwd 的 .env
#   > $DSH_HOME 的 .env
#
# 注意：启动环境是进程启动时拍下的快照，改完 .env 要重启进程才生效。

MODEL_GATEWAY_API_KEY=
