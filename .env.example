# yantao 模型网关配置模板。复制后填入真实值即可（`.env` 已被 .gitignore 忽略，本模板才入库）：
#   cp .env.example .env
#
# 术语见 CONTEXT.md: LLM 网关 / Model Gateway。路由按角色命名（provider 为
# `model-gateway`），下面这五项只是「当前用的是哪个实例」——换模型改这里，
# 不改代码。除密钥外全部可省略，省略即用括号内默认值。

# 1. 密钥（必填，唯一不该进库的东西）
MODEL_GATEWAY_API_KEY=

# 2. 网关地址（默认 https://aicodemarket.dahuatech.com/rd-soft-tools/glm/v1）
#    注意填到 /v1 这一级，不要带 /chat/completions
#MODEL_GATEWAY_URL=https://aicodemarket.dahuatech.com/rd-soft-tools/glm/v1

# 3. 模型 id（默认 GLM5.1）—— 必须是网关认得的那个 id
#MODEL_GATEWAY_MODEL_ID=GLM5.1

# 4. 模型的显示名（默认 inner-glm5.1）
#MODEL_GATEWAY_MODEL_NAME=inner-glm5.1

# 5. 网关在界面上的名字（默认 内网模型网关）
#MODEL_GATEWAY_NAME=内网模型网关

# 放置位置（二选一，或都写）：
#   1. <启动时的 cwd>/.env   —— 从本仓库目录启动时，就是本文件复制出的 .env
#   2. $DSH_HOME/.env        —— 默认 C:\Users\<你>\.dsh\.env，与启动目录无关
#
# 解析优先级（高 → 低）：
#   启动环境（export / 命令行前缀）
#   > $DSH_HOME/.credentials.yaml（UI「设置 → 模型」保存的，仅本用户可读）
#   > cwd 的 .env
#   > $DSH_HOME 的 .env
#
# 注意：启动环境是进程启动时拍下的快照，改完 .env 要重启进程才生效。
# 想同时挂多个模型，用 UI 的「设置 → 模型」或用户 patch 层，别往这里堆。
